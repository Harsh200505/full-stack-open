# Course Information

Full Stack Open exercises 1.1–1.2.

## Run locally

```bash
npm install
npm run dev
```

The application uses the components `App`, `Header`, `Content`, `Part`, and
`Total`. All course data is defined in `App` and passed to the other components
using props.

