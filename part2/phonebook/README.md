# Phonebook

Full Stack Open exercises 2.6–2.17.

The application supports filtering, creating, updating, and deleting contacts
through a JSON Server backend. It also displays timed success and error
notifications.

Start the backend in one terminal:

```bash
npm run server
```

Start the React application in another terminal:

```bash
npm run dev
```

