# Bloglist Frontend

Final frontend for Full Stack Open exercises 5.1–5.31.

## Run

Start the Part 4 backend on port 3003, then:

```bash
npm install
npm run dev
```

The Vite proxy forwards `/api` requests to `http://localhost:3003`.

## Verify

```bash
npm run lint
npm test
npm run build
```
